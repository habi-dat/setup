# Auth 2.0 (habidat-auth) - required variables for store/auth/auth.env
# Mapping from legacy user.env: HABIDAT_USER_* -> these names
# update.sh creates/updates auth.env from user.env + passwords.env (missing keys only)

# Database (same compose)
POSTGRES_PASSWORD=
DATABASE_URL=postgresql://postgres:PASSWORD@db:5432/habidat_auth
REDIS_URL=redis://redis:6379

# App URLs (from HABIDAT_PROTOCOL + HABIDAT_USER_SUBDOMAIN + HABIDAT_DOMAIN)
APP_URL=
NEXT_PUBLIC_APP_URL=
TRUSTED_ORIGINS=

# Secrets (from HABIDAT_USER_SESSION_SECRET or generate)
SESSION_SECRET=
BETTER_AUTH_SECRET=

# Seed (from passwords.env / user.env)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# LDAP (from user.env: HABIDAT_USER_LDAP_HOST:PORT, HABIDAT_USER_LDAP_BINDDN, etc.)
LDAP_URL=
LDAP_BIND_DN=
LDAP_BIND_PASSWORD=
LDAP_BASE_DN=
LDAP_USERS_DN=
LDAP_GROUPS_DN=

# SMTP (from user.env HABIDAT_USER_SMTP_*)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Nginx (from user.env)
VIRTUAL_HOST=
LETSENCRYPT_HOST=
CERT_NAME=

# Optional: Discourse (written by discourse/setup.sh and import.sh)
# DISCOURSE_URL= DISCOURSE_API_KEY= DISCOURSE_API_USERNAME= DISCOURSE_SSO_SECRET=

# Optional: Nextcloud (written by nextcloud/setup.sh and update.sh)
# NEXTCLOUD_* as needed by auth app

# Optional: SAML (certs can be file-based via /app/saml in container)
# SAML_PRIVATE_KEY= SAML_CERTIFICATE=
